name: Pruebas Unitarias

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  unit-tests:
    name: Pruebas Unitarias con Pytest
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          pip install -r requirements.txt
          pip install pytest-cov pytest-html

      - name: Ejecutar pruebas unitarias
        env:
          APP_ENV: test
        run: |
          python -m pytest tests/test_scanner.py tests/test_api.py \
            --cov=app \
            --cov-report=xml:coverage.xml \
            --cov-report=html:htmlcov/ \
            --cov-report=term-missing \
            -v \
            --tb=short \
            --junitxml=unit-results.xml \
            --html=unit-report.html --self-contained-html

      - name: Publicar resumen de pruebas
        if: always()
        run: |
          echo "## 🧪 Resultados de Pruebas Unitarias" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f unit-results.xml ]; then
            TESTS=$(grep -oP 'tests="\K[^"]+' unit-results.xml || echo "0")
            FAILURES=$(grep -oP 'failures="\K[^"]+' unit-results.xml || echo "0")
            ERRORS=$(grep -oP 'errors="\K[^"]+' unit-results.xml || echo "0")
            echo "| Métrica | Valor |" >> $GITHUB_STEP_SUMMARY
            echo "|---|---|" >> $GITHUB_STEP_SUMMARY
            echo "| Tests ejecutados | $TESTS |" >> $GITHUB_STEP_SUMMARY
            echo "| Fallos | $FAILURES |" >> $GITHUB_STEP_SUMMARY
            echo "| Errores | $ERRORS |" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Subir reporte de cobertura
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cobertura-unitarias
          path: |
            coverage.xml
            htmlcov/
            unit-results.xml
            unit-report.html
          retention-days: 15

      - name: Preparar reportes para GH Pages
        if: always()
        run: |
          mkdir -p public-unit
          if [ -f unit-report.html ]; then
            cp unit-report.html public-unit/index.html
          else
            echo "<h1>Reporte unitario no disponible</h1>" > public-unit/index.html
          fi
          if [ -d htmlcov ]; then
            cp -r htmlcov public-unit/coverage
          fi

      - name: Deploy Reporte Unitario a GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public-unit
          destination_dir: unit
          keep_files: true

      - name: Generar e integrar Index Dashboard
        if: always()
        run: |
          python3 scripts/generate_index.py

      - name: Deploy Dashboard Index
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public-root
          keep_files: true
